[pytest]
testpaths = tests trustchain_ig/tests
python_files = test_*.py
addopts = -q --strict-markers --disable-warnings
markers =
    security: security-focused regression and adversarial tests
    integration: integration-level tests across modules
